# Odoo server URL (for chat API)
NEXT_PUBLIC_ODOO_URL=http://localhost:8069

# (Optional) API key – must match ai_chat.config > api_secret_key in Odoo
# Leave empty if no key is configured on the Odoo side
NEXT_PUBLIC_ODOO_CHAT_API_KEY=
